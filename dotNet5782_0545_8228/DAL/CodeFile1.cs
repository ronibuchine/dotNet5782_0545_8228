using System;

namespace IDAL
{
    namespace DO
    {
        enum WeightCategories { heavy, medium, light };
        enum DroneStatuses { free, maintenance, delivery };
        enum Priorities { regular, fast, emergency };
    }
}
